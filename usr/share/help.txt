OpenTTY v&version (&system) Python Utilities.
OpenTTY (C) 2023 - Copyrighted by "Mr. Lima"
Licensed under MIT. See source distribution in GitHub
repository for more informations.

Usage: python -m opentty [--admin - Start as ROOT]
   or: python -m opentty [-b :: Active experiments daemon]
   
	OpenTTY is a Terminal Emulator that combine many common Unix 
	utilities in a single Python Module. The shell in this build
	is configured to run built-in utilities without $PATH search, 
	only considering the Assets in profile directory. 

Built-in commands:
	(( expression )); add-repo; alias; arch; asset; app; basename; bg;
	bc; bind; build; builtin; block; cal; cat; chmod; chown; chroot; 
	clear; cp; cpm; clone; catbin; connect; curl; cron; command; case;
	date; diff; dd; df; du; dumpsys; dir; del; declare; diff; dist; dm;
	dog; echo; exit; eval; exec; enable; export; env; expand; etk; find;
	false; function; fw; feed; fold; gen; genn; genip; gzip; grep; gear; 
	get; gping; gt; gaddr; getopt; getty; head; hash; help; hostname; 
	hostid; install; insmod; ifconfig; initd; idle; id; join; json; kill;
	ls; lsattr; login; ll; logname; mkdir; md; misc; mode; mount; more;
	mt; mv; merge; mod; mapinfo; netstat; np; nm; nl; nuke; no; nldd;
	open; od; path; ping; passwd; paste; patch; popd; ps; popon; pull;
	pwd; public; pub; poweroff; part; reset; rm; rmdir; route; rmmod; 
	run; rss; resume; rev; read; realpath; return; reboot; start; sleep;
	stty; sh; sort; sync; su; sleep; stdin; stdout; share; seq; sed; 
	set; source; suspend; stat; system; setup; tac; tar; tail; tap; tr; 
	thread; time; timeout; true; touch; test; type; tty; ttysize; trap; 
	unalias; unmount; uname; unzip; untar; unexpand; usleep; var; vi; 
	version; vip; vkss; wc; wait; watch; wget; whoami; which; warn;
	xargs; xxd; xml; xz; yes; zipinfo
